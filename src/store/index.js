import Vue from 'vue';
import Vuex from 'vuex';
import { isMobile } from 'mobile-device-detect';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    isMobile
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
